import React from "react"
import Cart from "../cart/Cart"

const Header=()=>{

    return(
        <header className="App-header">
        <section>shop name</section>
        <Cart/>
       
    </header>
    )
}

export default Header